"use client";import {useState} from "react";import {useRouter} from "next/navigation";
export default function Login(){const [password,setPassword]=useState(""),[error,setError]=useState("");const router=useRouter();async function go(e){e.preventDefault();const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({password})});const d=await r.json();if(!r.ok)return setError(d.error);router.replace("/admin");router.refresh()}return <main className="login"><form className="loginbox" onSubmit={go}><div className="mark">AS</div><h1>Back office</h1><p className="muted">Aadland Service</p><div className="field"><label>Admin-passord</label><input type="password" autoFocus value={password} onChange={e=>setPassword(e.target.value)}/></div>{error&&<p className="notice">{error}</p>}<button className="btn" style={{width:"100%",marginTop:14}}>Logg inn</button></form></main>}
