"use client";import {useState} from "react";import {createClient} from "@supabase/supabase-js";
export default function NyttPassord(){const [password,setPassword]=useState(""),[message,setMessage]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);async function go(e){e.preventDefault();setBusy(true);setError("");try{const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!url||!key){setError("Passordendring er ikke konfigurert.");return}const s=createClient(url,key);const {error}=await s.auth.updateUser({password});if(error){setError("Lenken er utløpt eller ugyldig. Be om en ny lenke fra Min side.");return}setMessage("Passordet er endret. Du kan nå gå tilbake og logge inn.")}finally{setBusy(false)}}return <main className="login"><form className="loginbox" onSubmit={go}><h1>Nytt passord</h1><p className="muted">Velg et nytt passord til kundekontoen din.</p><div className="field"><label>Nytt passord</label><input type="password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)}/></div>{error&&<p className="notice">{error}</p>}{message&&<p className="success">{message}</p>}<button className="btn" disabled={busy} style={{width:"100%",marginTop:14}}>{busy?"Lagrer …":"Lagre nytt passord"}</button><a href="/min-side" className="btn alt" style={{display:"block",textAlign:"center",marginTop:10}}>Til Min side</a></form></main>}